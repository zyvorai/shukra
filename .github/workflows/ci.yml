name: CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.25.x"
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Go tests
        run: go test -count=1 ./...
      - name: Generate CO-RE objects when clang and BTF exist
        run: make generate
      - name: Console tests
        run: npm --prefix web ci && npm --prefix web test
      - name: Console build
        run: npm --prefix web run build
